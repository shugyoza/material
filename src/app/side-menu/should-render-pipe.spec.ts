import { ShouldRenderPipe } from './should-render-pipe';

describe('ShouldRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ShouldRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
